<UserControl x:Class="BTimeLogger.Wpf.Controls.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BTimeLogger.Wpf.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel DataContext="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type local:Home}}}">
        <ListView ItemsSource="{Binding ViewModel.ReportLocations, Mode=OneWay}"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  VerticalAlignment="Top"
                  Name="listView">
            <!--<ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type }">
                <TextBlock Text="{Binding }"
                    <local:IntervalListItem ViewModel="{Binding }" />
                </DataTemplate>
            </ListView.ItemTemplate>-->
        </ListView>
        <Button Command="{Binding ViewModel.LoadCommand}">Load</Button>
        <TextBlock>Home</TextBlock>
    </StackPanel>
</UserControl>
